name: "Custom CodeQL Configuration"
disable-default-queries: true  # Soluciona el conflicto con setup avanzado

paths:
  include:
    - "**/*.py"
    - "**/*.yml"
  exclude:
    - "**/tests/**"
    - "**/mock/**"
    - "**/node_modules/**"

queries:
  - uses: security-and-quality
